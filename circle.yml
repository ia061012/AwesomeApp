machine:
        java:
                version: openjdk7
                enviroment:
                        ANDROID_HOME: /usr/local/android-sdk-linux


test:
        override:
                -echo "Nothing to do here"
        override:
                - ./gradlew test
deployment:
        master:
                branch: master
                commands:
                        - ./gradlew asembleRelease
